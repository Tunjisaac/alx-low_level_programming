#include "main.h"
/**
 * add - a function that adds two integers and returns the result
 * @_first: first number input
 * @_second: second number input
 * Return: first + second
 */
int add(int _first, int _second)
{
	return (_first + _second);
}
